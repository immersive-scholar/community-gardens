import TypographyTemplate from "../templates/TypographyTemplate";

class AnticipationSlide extends TypographyTemplate {
  constructor(props) {
    super(props);

    const title = "ANTICIPATE";
    this.state = { title };
  }
}

export default AnticipationSlide;
